export type UrlConfig = {
  onemap: string;
  tourismHub: string;
  lta: string;
};

export const urlConfig: UrlConfig = {
  onemap: 'https://www.onemap.gov.sg/api',
  tourismHub: 'https://api.stb.gov.sg',
  lta: 'http://datamall2.mytransport.sg/ltaodataservice',
};
